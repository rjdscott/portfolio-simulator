# Core dependencies — install these first
# pip install -r requirements.txt

# Data acquisition
yfinance>=0.2.40
pandas>=2.2.0
pandas-datareader>=0.10.0
lxml>=5.0.0
html5lib>=1.1

# Numeric compute
numpy>=1.26.0

# Arrow / Parquet (Phase 2 + 2.5)
pyarrow>=14.0.0

# Progress and CLI
tqdm>=4.66.0

# System introspection
psutil>=5.9.0

# DuckDB result registry + Polars (Phase result management)
duckdb>=0.10.0
polars>=0.20.0

# Phase 4: Distributed compute engines (optional — install as needed)
# uv add pyspark "dask[distributed]" "ray[default]"
pyspark>=3.5.0
dask[distributed]>=2024.1.0
ray[default]>=2.9.0

# Large optional installs — install with exact commands below:
# uv pip install torch --index-url https://download.pytorch.org/whl/cpu
# uv pip install "jax[cpu]"
